name: CI

on:
  push:
    branches: [main]
  pull_request:

jobs:
  backend:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: backend

    services:
      postgres:
        image: postgres:16
        env:
          POSTGRES_USER: sukuu
          POSTGRES_PASSWORD: sukuu
          POSTGRES_DB: sukuu_test
        ports:
          - 5432:5432
        options: >-
          --health-cmd pg_isready
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5

    env:
      DATABASE_URL: postgresql+psycopg://sukuu:sukuu@localhost:5432/sukuu_test
      # Must be at least 32 bytes or PyJWT warns on every token issued.
      # Quoted, and not all-digits: YAML parses a bare 000...0 as the integer
      # 0, which reaches the app as the one-character string "0".
      JWT_SECRET: "ci-only-not-a-real-secret-0123456789abcdef"
      STRIPE_SECRET_KEY: sk_test_dummy
      STRIPE_WEBHOOK_SECRET: whsec_dummy

    steps:
      - uses: actions/checkout@v7

      - uses: actions/setup-python@v7
        with:
          python-version: "3.14"
          cache: pip
          cache-dependency-path: backend/requirements-dev.txt

      - name: Install dependencies
        run: pip install -r requirements-dev.txt

      - name: Lint
        run: ruff check .

      - name: Check formatting
        run: ruff format --check .

      - name: Migrations are reversible
        # Autogenerate does not drop Postgres enum types on downgrade, so a
        # broken downgrade only shows up on the *second* upgrade. Cycling twice
        # is what catches it.
        run: |
          alembic upgrade head
          alembic downgrade base
          alembic upgrade head
          alembic downgrade base

      - name: Seed script runs
        # Reset afterwards so the test run starts from an empty schema. The
        # tests are written not to care either way, but leaving seed rows in
        # place would quietly change what they exercise.
        run: |
          alembic upgrade head
          python -m app.db.seed
          alembic downgrade base
          alembic upgrade head

      - name: Test
        run: pytest --cov --cov-report=term-missing
